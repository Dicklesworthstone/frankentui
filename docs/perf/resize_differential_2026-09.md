# Resize coalescing: BOCPD vs the rate heuristic (2026-09-20)

Commit `e4348fd0`, seed `104372001701892`, final-apply budget 40 ms. Generated by `scripts/resize_storm_differential_e2e.sh`; rerun it to regenerate this file.

Both detectors are driven over **identical** event schedules on a virtual clock, so these numbers measure the detectors and not the machine. `frames_during_drag` counts `ApplyResize` actions before the last event — the repaints coalescing exists to avoid. `final_apply_latency_ms` is the time from the last event until the resize settles on its size — what coalescing costs.

| Pattern | frames during drag (heur → bocpd) | final apply ms (heur → bocpd) | regime transitions (heur → bocpd) | posterior decided | verdict |
|---|---|---|---|---|---|
| `burst_50` | 6 → **7** | 33 → **49** | 1 → 2 | 49 | REGRESSION |
| `burst_200` | 26 → **27** | 24 → **0** | 1 → 2 | 199 | REGRESSION |
| `sweep_80x24_to_200x60` | 10 → **12** | 40 → **24** | 1 → 2 | 39 | REGRESSION |
| `oscillate_10` | 4 → **7** | 52 → **52** | 1 → 2 | 19 | REGRESSION |
| `mixed_100` | 25 → **33** | 45 → **45** | 1 → 13 | 88 | REGRESSION |
| `pathological_50` | 20 → **22** | 51 → **19** | 4 → 22 | 30 | REGRESSION |

The *posterior decided* column is the validity check: it counts decisions BOCPD made in the bocpd run. A zero would mean the bocpd column had silently fallen back to the heuristic, making every verdict here a comparison of the heuristic against itself. The heuristic-only runs record zero BOCPD decisions, as `without_bocpd()` requires.

## Verdict

**On the frames criterion, BOCPD loses on 6 of 6 patterns.** The plan's rule for that case is to flip `enable_bocpd` back to `false` rather than relax the bar:

- `burst_50`: REGRESSION
- `burst_200`: REGRESSION
- `sweep_80x24_to_200x60`: REGRESSION
- `oscillate_10`: REGRESSION
- `mixed_100`: REGRESSION
- `pathological_50`: REGRESSION

**But the plan states two criteria, and they disagree here.** The second is that the final size lands within 40 ms of the last event:

- heuristic is over budget on **3 of 6**: `oscillate_10`, `mixed_100`, `pathological_50`
- bocpd is over budget on **3 of 6**: `burst_50`, `oscillate_10`, `mixed_100`

BOCPD is no better on the settling criterion either, so the two criteria agree and the plan's rule applies without a judgement call.

## Reading the numbers

A pattern where both detectors draw the same number of frames is not a failure of the experiment: the hard deadline and the latest-wins rule do most of the work on schedules that never leave one regime, and the detector only decides how long to hold. The patterns that separate them are the ones that change regime mid-storm.

Replay exit status: 101 (0 = every check passed).
