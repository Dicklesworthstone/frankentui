# Modules that are declared `pub mod` but reached by nothing in production.
#
# THIS LIST MAY ONLY SHRINK. Add a line only with a bead id, and only for a
# module that bead is going to wire up or quarantine. If a module on this list
# becomes reachable, scripts/check_module_reachability.py fails with
# STALE_ALLOWLIST, so an entry cannot quietly outlive its reason.
#
# Seeded 2026-09-18 for bd-g00-root-epic-ewths.11.2 from the tree at 087ff3d9.
# Every entry below was cross-checked with ripgrep: the only apparent
# counter-example, doctor_frankentui::lowering, is referenced solely from a
# `#[cfg(test)] mod tests` block in ir_normalize.rs, which is not production
# reachability.
#
# Bead ids here are the beads that own the *decision*, not always the eventual
# wiring. Re-point an entry at a more specific bead as soon as one exists.
#
# The bead must still be OPEN. The gate reads .beads/issues.jsonl and fails on
# an entry whose bead is closed or unknown. A closed bead will never wire or
# quarantine anything, so an entry pointing at one is stranded with no owner and
# no one looking - which is how .11.3's widgets and .11.5's harness modules both
# sat here unowned until someone happened to check the bead rather than the line.
#
# Format: <crate>::<module>  # <bead id>
#
# NOT on this list: modules whose only consumers are tests. The gate reports
# those as TEST_ONLY, because "nothing uses this" and "only tests use this"
# are different states and a dead-code list that conflates them is dangerous.
# On 2026-09-19, 18 of this file's 48 entries turned out to be test
# infrastructure with real test consumers - ftui_render::headless alone is 843
# lines of documented CI harness used by two test files. Deleting any of them
# under the .11.5 permission would have broken tests. Run
# `make reachability` and read the TEST_ONLY lines before deleting anything.

# ── doctor_frankentui ────────────────────────────────────────────────────
# .28.1 asks the owner to split or feature-gate doctor_frankentui's three
# products. Until that is answered, every unreached module here is part of the
# same question rather than an independent orphan. The *_tests modules are
# `pub mod`s holding test code, which is its own smell and belongs to the same
# decision.
doctor_frankentui::alien_kernel_unit_tests  # bd-g00-root-epic-ewths.28.1
doctor_frankentui::archetype_scoreboard  # bd-g00-root-epic-ewths.28.1
doctor_frankentui::artifact_coding_tests  # bd-g00-root-epic-ewths.28.1
doctor_frankentui::backend_capability  # bd-g00-root-epic-ewths.28.1
doctor_frankentui::cli_explain_tests  # bd-g00-root-epic-ewths.28.1
doctor_frankentui::corpus_integrity_tests  # bd-g00-root-epic-ewths.28.1
doctor_frankentui::governance_validator_tests  # bd-g00-root-epic-ewths.28.1
doctor_frankentui::graveyard_control_tests  # bd-g00-root-epic-ewths.28.1
doctor_frankentui::migration_config  # bd-g00-root-epic-ewths.28.1
doctor_frankentui::optimization_control_tests  # bd-g00-root-epic-ewths.28.1
doctor_frankentui::optimization_model_tests  # bd-g00-root-epic-ewths.28.1
doctor_frankentui::portfolio_governance_tests  # bd-g00-root-epic-ewths.28.1
doctor_frankentui::tier_escalator_tests  # bd-g00-root-epic-ewths.28.1

# ── ftui-web ─────────────────────────────────────────────────────────────
# .29.1 asks the owner to decide the web host's scope. These are the modules
# that scope would either wire into a host or drop.
ftui-web::keyboard  # bd-g00-root-epic-ewths.29.1
ftui-web::pointer  # bd-g00-root-epic-ewths.29.1
# Encodes the renderer tier order (dom/canvas/webgl/webgpu), browser support
# matrix and release criteria that the README's front-page browser claim rests
# on - and nothing calls it, so none of that policy is enforced in-tree. Note
# ftui-web "does not bind to wasm-bindgen yet" (lib.rs) and the actual
# WebGPU/canvas2d rendering lives in the out-of-tree frankenterm-web bundle,
# so this module is the only in-tree statement of that contract.
ftui-web::release_readiness  # bd-g00-root-epic-ewths.29.1

# ── ftui-widgets ─────────────────────────────────────────────────────────
# popover, pretty and receipt_verifier_panel were here with .11.3 as their
# owner; that bead closed without covering them, so they had no owner left.
# Gallery section "J: Verification" now renders all three and
# widget_gallery_verification_120x40 pins the result, so they are gone from
# this list rather than re-pointed at another bead.
#
# choreography is NOT a widget and never belonged with them: it has no Widget
# impl at all. It is a protocol/choreography specification verifier (project(),
# verify_deadlock_free(), completeness_report()) that happens to live in
# ftui-widgets. Wiring it into a widget gallery would be the wrong fix; it
# needs the delete-or-wire decision with the rest of the orphan set.
ftui-widgets::choreography  # bd-gqit2
# (ftui-widgets::fenwick sat here until the TEST_ONLY verdict landed and its
# proptest counted as a consumer. It is still a duplicate of virtualized.rs's
# own VariableHeightsFenwick (:741); that observation moved to bd-gqit2 rather
# than staying as a comment with no entry under it.)

# ── orphan-module candidates (G07) ───────────────────────────────────────
# .11.5 granted deletion for an enumerated set on 2026-09-18 and then closed.
# None of the modules below are on that list, so its permission does not reach
# them and its closure left them ownerless. bd-gqit2 picked up the four that
# were pointing at it and carries the per-module analysis.
# (event_coalescer was wired into Program in bd-tu3l9).
# Superseded, not merely unused: this is a second Esc-Esc interpreter (708
# lines, bd-2vne.2). The one that actually runs is keybinding::SequenceDetector,
# which KeyDispatcher owns (keybinding.rs:775), constructs (:782) and feeds Esc
# through (:835) - so the README's "Esc Esc goes through the SequenceDetector
# window" is true, just not via this module. Merge or remove rather than wire.
ftui-core::key_sequence  # bd-gqit2
# Unreachable by decision, not neglect: benchmarked 2026-09-18 at 4-19x slower
# than Flex::split on the same constraints, so wiring it in would cost every
# layout pass. Evidence and reasoning in docs/perf/egraph_vs_flex_2026-09-18.md.
ftui-layout::egraph  # bd-gqit2
# Not neglected — filed in the wrong crate for its own use case. Its doc says
# it pays off for ">1M rows, <1% dirty"; ftui-render tracks dirty rows as
# Buffer::dirty_rows: Vec<bool> sized to terminal height (buffer.rs:226), which
# never approaches that. The workload it was written for lives in a virtualized
# list widget. Its "How It Works" also describes 3-bit slot metadata that its
# own Implementation Note says it does not use.
ftui-render::quotient_filter  # bd-gqit2

# ── ftui-harness ─────────────────────────────────────────────────────────
# Reached by neither production code nor any test, which for a test-support
# crate means nothing uses them at all. Two left this list on 2026-09-19:
# cost_surface, when render_gauntlet started using it for stage attribution,
# and frame_comparison, when ftui-layout's golden_incremental test started
# using it for cell-level divergence reports.
#
# These pointed at .11.5, which is closed and enumerates ftui-render and
# ftui-runtime files — it never covered ftui-harness, so these had no owner.
# bd-5h57k is that owner. They divide into two kinds, and the wire-or-retire
# answer differs for each, which is why one blanket question produced none.
#
# Engines — complete, tested analyzers with a callable API, waiting on a caller:
ftui-harness::doctor_topology  # bd-5h57k
ftui-harness::hotspot_extraction  # bd-5h57k
ftui-harness::optimization_policy  # bd-5h57k
ftui-harness::proof_oracle  # bd-5h57k
#
# Reference models — taxonomies encoded as enums, with 0-2 public functions
# between them. There is nothing here to call, so "wiring" them means writing
# the checker they describe, not connecting an existing one.
ftui-harness::layout_reuse  # bd-5h57k
ftui-harness::presenter_equivalence  # bd-5h57k
ftui-harness::rollout_runbook  # bd-5h57k
