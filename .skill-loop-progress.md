# Skill Loop Progress
# Skill: extreme-software-optimization
# Target: /data/projects/frankentui
# Total Passes: 10
# Started: 2026-06-09T02:05:00Z

## Status: COMPLETE - 10 optimization passes applied and final profile captured

## Profiling Handoff

- Run directory: `tests/artifacts/perf/20260609T015418Z-profile-optimization/`
- Baseline commit: `d35c42cd`
- Primary workload: `/data/tmp/cargo-target/release-perf/profile_sweep --cycles 200 --render-mode pipeline --arena-mode off --json`
- Baseline p95/p99: `701us` / `1479us`
- Primary hotspot: repeated `Paragraph::new` / `Text::into_owned` allocation churn.
- Required mid-loop refresh: after pass 5, run a fresh `profiling-software-performance` profile before pass 6.

## Missions

1. Chrome static paragraph churn: remove repeated per-frame `Paragraph::new` / `Text::into_owned` allocations in tab/status chrome without changing rendered content.
2. Markdown math cache: make repeated LaTeX-to-Unicode conversion avoid `unicodeit::replace` on cache hits, with bounded deterministic behavior.
3. Performance HUD allocation tail: reduce the `performance_hud` screen's p95 allocation count while preserving displayed values and ordering.
4. Dashboard stable text reuse: reduce per-frame string/text construction in dashboard panels and other stable summary sections found in the profile.
5. Paragraph/text ownership fast path: add or use a narrow API path that avoids owned text conversion when callers already have static/borrowed text.
6. Quake/visual effects frame tail: after re-profiling, attack the top CPU/alloc issue in `quake_easter_egg` or `visual_effects`.
7. Markdown rich/live editor render cache: after re-profiling, reduce repeated markdown render work that remains hot without changing snapshots.
8. Width/grapheme repeated calculation: after re-profiling, cache or avoid repeated width/grapheme work only where the refreshed profile keeps it top-ranked.
9. Render kernel micro-tuning: after re-profiling, apply one low-risk buffer/diff/presenter lever only if it remains score >= 2.0.
10. Final tail pass: use the latest profile data to remove one remaining low-hanging allocation or CPU hotspot and run full verification.

## Completed Passes

### Pass 1 - Chrome static paragraph churn - 2026-06-09T03:59:00Z
- Files changed: `crates/ftui-demo-showcase/src/chrome.rs`, `.skill-loop-progress.md`, `tests/artifacts/perf/20260609T015418Z-profile-optimization/pass1_profile_sweep_cycles100.json`
- Changes: Replaced tab/status chrome one-line `Paragraph::new(Text::from_lines(...))` construction with chrome-local direct span rendering into the frame buffer, avoiding the eager `Text::into_owned` path on every frame.
- Evidence: `heaptrack_profile_sweep_cycles50_print.txt` ranked `Paragraph::new` / `Text::into_owned` allocation churn first, including tab/status chrome stacks.
- Proof: Ordering preserved because spans are rendered left-to-right in the same order; tie-breaking is unchanged; floating-point and RNG are not involved; 291 demo screen snapshots passed unchanged.
- Measurement: post-pass `profile_sweep --cycles 100` kept behavior metrics stable and improved allocations/frame from baseline p50/p95 `259/966` to `163/857`.
- Verification: `cargo fmt --check`; `cargo check -p ftui-demo-showcase --all-targets`; `cargo test -p ftui-demo-showcase chrome`; `cargo test -p ftui-demo-showcase --test screen_snapshots`; `cargo clippy -p ftui-demo-showcase --all-targets -- -D warnings`; `ubs crates/ftui-demo-showcase/src/chrome.rs .skill-loop-progress.md`.
- Commit: `78e6bd0c`
- Verdict: PRODUCTIVE

### Pass 2 - Shared markdown math cache - 2026-06-09T03:28:00Z
- Files changed: `crates/ftui-extras/src/markdown.rs`, `.skill-loop-progress.md`
- Changes: Added a bounded process-level `LatexMathCache` behind `OnceLock<Mutex<_>>` so fresh markdown renderers can reuse pure LaTeX-to-Unicode conversions before calling `unicodeit::replace`; renderer-local cache remains the first lookup and is hydrated from the shared cache on shared hits.
- Evidence: `hotspot_table.md` ranked markdown math conversion third, with `159,611` allocation calls through `unicodeit::replace`, `latex_to_unicode`, and `cached_latex_to_unicode`.
- Proof: Conversion is deterministic and keyed solely by the LaTeX string; both local and shared caches keep the existing `LATEX_CACHE_CAPACITY` bound; poisoned locks fall back to direct conversion instead of blocking rendering; the new unit test proves a fresh renderer reads the shared cache and cleans up its sentinel entry.
- Measurement: No dedicated post-pass runtime profile yet; this targets the repeated-renderer allocation stack and will be remeasured in the required post-pass-5 profiling refresh.
- Verification: `rch exec --json -- cargo check -p ftui-extras --features markdown --all-targets`; `rch exec --json -- cargo test -p ftui-extras --features markdown markdown::tests:: -- --nocapture`; `RCH_REQUIRE_REMOTE=1 rch exec --json -- cargo clippy -p ftui-extras --features markdown --all-targets -- -D warnings`; `git diff --check`; `ubs --only=rust crates/ftui-extras/src/markdown.rs .skill-loop-progress.md` completed with no critical findings, but UBS internally ran local shadow-workspace Cargo phases and is not counted as remote Cargo proof.
- Commit: `2347fc51`
- Verdict: PRODUCTIVE

### Pass 3 - Performance HUD allocation tail - 2026-06-09T04:03:00Z
- Files changed: `crates/ftui-demo-showcase/src/screens/performance_hud.rs`, `.skill-loop-progress.md`
- Changes: Replaced the sparkline hot loop's one-cell `String::from(ch)` plus `Paragraph::new(...).render(...)` path with direct buffer cell writes that apply the same style and degradation semantics, avoiding per-cell string/text/paragraph allocation.
- Evidence: `top_screens_by_alloc_p95.tsv` ranked `performance_hud` first at `6,191` p95 allocations/frame; heaptrack references point into `performance_hud.rs` around the per-cell sparkline path; `hotspot_table.md` scored this lever `8.0`.
- Proof: Values/min/max/range/fill/BRAILLE selection are unchanged; column/row iteration order and coordinates are unchanged; status/axis/layout/stat paths are untouched; direct write preserves degradation content/styling behavior and `set_fast` falls through to `set()` for non-base scissor/opacity.
- Measurement: Subagent remote profile sweep reported `performance_hud` p95 allocations/frame `294` for `profile_sweep --cycles 100 --render-mode pipeline --arena-mode off --json`; full refreshed profile still required after pass 5.
- Verification: `rustfmt --edition 2024 --check crates/ftui-demo-showcase/src/screens/performance_hud.rs`; `RCH_REQUIRE_REMOTE=1 rch exec --json -- cargo check -p ftui-demo-showcase --all-targets`; `RCH_REQUIRE_REMOTE=1 rch exec --json -- cargo test -p ftui-demo-showcase performance_hud -- --nocapture`; `RCH_REQUIRE_REMOTE=1 RCH_WORKER=ovh-a rch exec --json -- cargo clippy -p ftui-demo-showcase --all-targets -- -D warnings`; `RCH_REQUIRE_REMOTE=1 RCH_WORKER=ovh-a rch exec --json -- cargo test -p ftui-demo-showcase --test screen_snapshots performance_hud -- --nocapture`; `git diff --check`. UBS was not rerun because its Rust module invokes local Cargo phases.
- Commit: `bd73e20b`
- Verdict: PRODUCTIVE

### Pass 4 - Dashboard stable text reuse - 2026-06-09T04:24:00Z
- Files changed: `crates/ftui-demo-showcase/src/screens/dashboard.rs`, `.skill-loop-progress.md`
- Changes: Replaced the dashboard panel hint/footer `Paragraph::new(truncate_to_width(...))` path with a dashboard-local direct single-line renderer, avoiding repeated stable hint/footer `String` truncation and `Paragraph`/`Text::into_owned` churn.
- Evidence: baseline artifacts ranked `Paragraph::new` to `Text::into_owned` first at `439,134` allocation calls, and ranked `dashboard` at `1,350` p95 allocations/frame and `1,569us` p99 frame time; `hotspot_table.md` scored dashboard stable text precomputation at `8.0`.
- Proof: Call-site ordering is unchanged; text clipping uses the same `graphemes()` plus `grapheme_width()` cutoff as `truncate_to_width`; wide or multi-codepoint graphemes still use the frame grapheme pool; floating-point, RNG, timing, simulated data, and update logic are unchanged; the helper mirrors `Paragraph`'s clear-then-draw degradation/style behavior for one-line no-wrap hint areas, including no double-application of translucent backgrounds and stale link-id clearing.
- Measurement: subagent remote 50-cycle profile sweep reported cumulative dashboard p95 allocations/frame `1,194` versus saved baseline `1,350`, p95 frame time `834us` versus `1,128us`, and p95 bytes/frame `576,008` versus `584,050`; this is cumulative after passes 1-4, not isolated pass-4-only.
- Verification: `rustfmt --edition 2024 --check crates/ftui-demo-showcase/src/screens/dashboard.rs`; `RCH_REQUIRE_REMOTE=1 rch exec --json -- cargo check -p ftui-demo-showcase --all-targets`; `RCH_REQUIRE_REMOTE=1 RCH_WORKERS=ovh-a rch exec --json -- cargo test -p ftui-demo-showcase --test screen_snapshots dashboard_initial_120x40 -- --nocapture`; `RCH_REQUIRE_REMOTE=1 rch exec --json -- cargo clippy -p ftui-demo-showcase --all-targets -- -D warnings`; `git diff --check`. One snapshot retry selected broken worker `vmi1149989` and failed closed with `remote required; refusing local fallback`; UBS was not run because its Rust module invokes local Cargo phases.
- Commit: `667438e5`
- Verdict: PRODUCTIVE

### Pass 5 - Paragraph/text ownership fast path - 2026-06-09T05:02:00Z
- Files changed: `crates/ftui-widgets/src/paragraph.rs`, `crates/ftui-demo-showcase/src/screens/performance_hud.rs`, `.skill-loop-progress.md`
- Changes: Added `Paragraph::from_static_text` and `Paragraph::from_static_str` so already-static text can satisfy the widget's existing `FtuiText<'static>` storage model without `text_into_owned`; switched the static Performance HUD title to the new fast path.
- Evidence: `heaptrack_profile_sweep_cycles50_print.txt` ranked `Paragraph::new` -> `text_into_owned` -> `Span::into_owned` first at `439,134` allocation calls; the pass targets the saved top allocation stack with opportunity score `10.0`.
- Proof: Existing `Paragraph::new` behavior is unchanged and still owns arbitrary lifetimes; the new constructors accept only `FtuiText<'static>` or `&'static str`, so borrowed spans cannot dangle; `Text::raw(&'static str)` splits into borrowed line slices; render ordering, style, wrap, alignment, and scroll defaults share the same constructor helper; floating-point, RNG, and timing paths are untouched; the focused test compares rendered rows against `Paragraph::new` and proves borrowed span storage.
- Measurement: No fresh profile in-pass; the required post-pass-5 profile refresh follows before pass 6.
- Verification: `rustfmt --edition 2024 --check crates/ftui-widgets/src/paragraph.rs crates/ftui-demo-showcase/src/screens/performance_hud.rs`; `RCH_REQUIRE_REMOTE=1 rch exec --json -- cargo test -p ftui-widgets --lib from_static_str_preserves_rendering_without_owning_static_spans`; `RCH_REQUIRE_REMOTE=1 rch exec --json -- cargo check -p ftui-widgets -p ftui-demo-showcase --all-targets`; `RCH_REQUIRE_REMOTE=1 rch exec --json -- cargo clippy -p ftui-widgets -p ftui-demo-showcase --all-targets -- -D warnings`; `git diff --check`. Initial validation attempts against `vmi1149989`, `vmi1152480`, `vmi1153651`, and `vmi1293453` failed closed due remote topology preflight errors; no local fallback occurred. UBS was not run because it invokes local Cargo phases.
- Commit: `81b4d54d`
- Verdict: PRODUCTIVE

## Post-Pass-5 Profiling Refresh - 2026-06-09T05:26:00Z

- Run directory: `tests/artifacts/perf/20260609T051942Z-post-pass5-refresh/`
- Command: `RCH_REQUIRE_REMOTE=1 rch exec --json -- cargo run --profile release-perf -p ftui-demo-showcase --bin profile_sweep -- --cycles 200 --render-mode pipeline --arena-mode off --json`
- Remote worker: `vmi1227854`; no local fallback occurred. Earlier attempts against unhealthy or saturated workers failed closed.
- Summary: `18,000` frames, elapsed `6818.309ms`, throughput `2639.95 renders/sec`, frame p50/p95/p99 `270/992/2264us`, allocation p50/p95/p99 `163/773/1194`, bytes p95 `524,866`, presenter p95/p99 `84/135us`.
- Refreshed allocation tail: `markdown_rich_text` p95 `1225`, `dashboard` p95 `1194`, `code_explorer` p95 `954`, `table_theme_gallery` p95 `855`, `theme_studio` p95 `827`.
- Refreshed frame-time tail: `visual_effects` p95 `1793us`, `quake_easter_egg` p95 `1720us`, `dashboard` p95 `1657us`, `markdown_rich_text` p95 `1306us`.
- Pass-6 target: attack the current VFX/Quake frame-time tail first unless code inspection finds the low-hanging change is too risky; otherwise use `markdown_rich_text` allocations as the next target.

### Pass 6 - Quake/visual effects frame tail - 2026-06-12T16:11:00Z
- Files changed: `crates/ftui-extras/src/canvas.rs`, `crates/ftui-extras/src/visual_fx/effects/canvas_adapters.rs`, `crates/ftui-demo-showcase/src/screens/quake.rs`.
- Changes: Added `Painter::point_colored_sparse_at_index_in_bounds` and switched dense sparse renderers in Metaballs and the Quake lightmap loop to the branch-free setter.
- Evidence: The post-pass-5 refresh ranked `visual_effects` and `quake_easter_egg` as the top frame-time p95 tails at `1793us` and `1720us`, with low allocation counts, so their remaining tail was CPU-heavy per-pixel raster work.
- Proof: The new setter writes the same `pixels[idx] = generation` and `colors[idx] = Some(color)` state as the checked index setter when full-coverage mode is inactive. The changed Metaballs and Quake call sites render sparse coverage and do not activate the full-coverage path before these writes; coordinate ordering, field/light math, thresholds, color calculations, RNG/tick inputs, and floating-point expressions are unchanged.
- Measurement: No fresh profile in-pass; this is a narrow hot-loop branch removal and will be reflected in later profile data.
- Verification: `rustfmt --edition 2024 --check crates/ftui-extras/src/canvas.rs crates/ftui-extras/src/visual_fx/effects/canvas_adapters.rs crates/ftui-demo-showcase/src/screens/quake.rs`; `git diff --check`; `RCH_REQUIRE_REMOTE=1 rch exec --json -- cargo test -p ftui-extras --features canvas --lib sparse_index_colored_point_matches_checked_index_path -- --nocapture`; `RCH_REQUIRE_REMOTE=1 rch exec --json -- cargo test -p ftui-demo-showcase --lib quake_render_visible -- --nocapture`; `RCH_REQUIRE_REMOTE=1 rch exec --json -- cargo check -p ftui-extras -p ftui-demo-showcase --all-targets --features ftui-extras/canvas,ftui-extras/visual-fx`; `RCH_REQUIRE_REMOTE=1 rch exec --json -- cargo clippy -p ftui-extras -p ftui-demo-showcase --all-targets --features ftui-extras/canvas,ftui-extras/visual-fx -- -D warnings`. Two attempted `ftui-extras --features visual-fx` test filters for Metaballs selected `0` tests and are not counted as proof; no local Cargo fallback occurred.
- Commit: `ae75a8fd`
- Verdict: PRODUCTIVE

### Pass 7 - Markdown rich/live editor render cache - 2026-06-12T16:26:49Z
- Files changed: `crates/ftui-demo-showcase/src/screens/markdown_rich_text.rs`.
- Changes: Added viewport-level caches for the static markdown panel and streaming markdown panel, keyed by width, scroll, and height, and rendered cached `Text<'static>` through `Paragraph::from_static_text` to avoid rewrapping visible markdown and re-owning spans on every frame.
- Evidence: The post-pass-5 refresh ranked `markdown_rich_text` as the highest allocation tail at p95 `1225` allocations/frame and p95 frame time `1306us`; code inspection found cached parsed markdown still flowed through per-frame `wrap_markdown_for_viewport(...)` and `Paragraph::new(...)`.
- Proof: Markdown parse/render order is unchanged; wrapping order is unchanged; viewport cache invalidation includes every input that affects visible markdown output (`width`, `scroll`, `height`), while the streaming cache still invalidates on `width`, stream position, and completion state. Floating-point, RNG, timing, markdown content, detection logic, cursor insertion, layout, and scrolling semantics are unchanged.
- Measurement: No fresh profile in-pass; this removes repeated viewport wrapping and owned-text conversion work from the profiled markdown-rich allocation tail.
- Verification: `rustfmt --edition 2024 --check crates/ftui-demo-showcase/src/screens/markdown_rich_text.rs`; `git diff --check -- crates/ftui-demo-showcase/src/screens/markdown_rich_text.rs`; `RCH_REQUIRE_REMOTE=1 rch exec --json -- cargo test -p ftui-demo-showcase --lib markdown_viewport_cache_matches_direct_wrap -- --nocapture`; `RCH_REQUIRE_REMOTE=1 rch exec --json -- cargo test -p ftui-demo-showcase --lib stream_viewport_cache_keys_scroll_and_height -- --nocapture`; `RCH_REQUIRE_REMOTE=1 rch exec --json -- cargo clippy -p ftui-demo-showcase --all-targets -- -D warnings`. One earlier remote clippy attempt failed on `too_many_arguments` and was fixed before the passing proof; no local Cargo fallback occurred.
- Commit: `912ffbf9`
- Verdict: PRODUCTIVE

### Pass 8 - Width/grapheme repeated calculation - 2026-06-12T16:36:36Z
- Files changed: `crates/ftui-demo-showcase/src/screens/code_explorer.rs`.
- Changes: Added `truncate_to_width_with_width` and used it in highlighted-match rendering plus `pad_to_width`, so clipping and rendered-width measurement share one grapheme scan instead of `truncate_to_width(...)` followed by `display_width(...)`.
- Evidence: The post-pass-5 refresh ranked `code_explorer` at p95 `954` allocations/frame and p95 frame time `786us`; code inspection found repeated render-time `truncate_to_width`, `display_width`, `grapheme_count`, and grapheme scan paths.
- Proof: Segment ordering is unchanged: before-match, match, and tail text still render in the same order. Clipping uses the same `graphemes()` and `grapheme_width()` logic as before; `truncate_to_width` delegates to the new helper, preserving existing string results; the returned width is exactly the displayed width of the clipped string, including wide and combining-grapheme cases. Search order, byte-boundary guards, scrolling, styling, floating-point timing, RNG/seed use, and layout math are unchanged.
- Measurement: No fresh profile in-pass; this removes duplicate grapheme/display-width scans in a profiled allocation-heavy screen.
- Verification: `rustfmt --edition 2024 --check crates/ftui-demo-showcase/src/screens/code_explorer.rs`; `git diff --check -- crates/ftui-demo-showcase/src/screens/code_explorer.rs`; `RCH_REQUIRE_REMOTE=1 rch exec --json -- cargo test -p ftui-demo-showcase --lib truncate_width_with_width_matches_rendered_width -- --nocapture`; `RCH_REQUIRE_REMOTE=1 rch exec --json -- cargo test -p ftui-demo-showcase --lib pad_to_width_uses_rendered_width_for_wide_graphemes -- --nocapture`; `RCH_REQUIRE_REMOTE=1 rch exec --json -- cargo test -p ftui-demo-showcase --lib code_explorer_search -- --nocapture`; `RCH_REQUIRE_REMOTE=1 rch exec --json -- cargo clippy -p ftui-demo-showcase --all-targets -- -D warnings`. Remote clippy encountered transient crates.io HTTP2/partial-download warnings but recovered and exited 0; no local Cargo fallback occurred.
- Commit: `db2de05d`
- Verdict: PRODUCTIVE

### Pass 9 - Render kernel micro-tuning - 2026-06-12T16:44:50Z
- Files changed: `crates/ftui-render/src/presenter.rs`.
- Changes: Added a single-run row fast path in `Presenter::emit_diff_runs`, skipping `plan_row_reuse` and `RowPlan`/`SmallVec` construction when a row contains exactly one `ChangeRun`; extracted the shared span emission body into `emit_row_span`.
- Evidence: The post-pass-5 profile showed presenter p95/p99 at `84us`/`135us` overall and `145us` p95 on `visual_effects`; code inspection showed every row paid row-plan construction even though `plan_row_reuse` deterministically maps a single run to a single identical span.
- Proof: Run ordering is unchanged because rows are still processed in `runs_buf` order. For single-run rows, the fast path emits the one run at the same point the one-span plan would emit it. Cursor positioning, style changes, hyperlink changes, content emission, wide-character tail repair, and frame cleanup share the same `emit_row_span`/`emit_cell` paths as planned emission; a focused test proves byte equivalence for styled linked single-run output. Floating-point, RNG, and tie-breaking are not involved.
- Measurement: No fresh profile in-pass; this removes per-row planning overhead for common single-run rows in the presenter hot path.
- Verification: `rustfmt --edition 2024 --check crates/ftui-render/src/presenter.rs`; `git diff --check -- crates/ftui-render/src/presenter.rs`; `RCH_REQUIRE_REMOTE=1 rch exec --json -- cargo test -p ftui-render --lib emit_diff_runs_single_run_matches_planned_span_output -- --nocapture`; `RCH_REQUIRE_REMOTE=1 rch exec --json -- cargo test -p ftui-render --lib plan_row_reuse_single_run_matches_plan_row -- --nocapture`; `RCH_REQUIRE_REMOTE=1 rch exec --json -- cargo clippy -p ftui-render --all-targets -- -D warnings`; no local Cargo fallback occurred.
- Commit: `00c4e744`
- Verdict: PRODUCTIVE

### Pass 10 - Final table gallery allocation tail - 2026-06-12T16:55:31Z
- Files changed: `crates/ftui-demo-showcase/src/screens/table_theme_gallery.rs`.
- Changes: Changed `TableThemeGallery::table_rows()` from a `Vec<Row>` fixture to a fixed `[Row; 4]` fixture so every preset card and preview render can feed `Table::new` without allocating an intermediate row vector.
- Evidence: The post-pass-5 refresh ranked `table_theme_gallery` fourth by allocation tail at p95 `855` allocations/frame and bytes p95 `595,066`; code inspection found `table_rows()` allocated a fresh vector for every preset card and widget preview even though `Table::new` already accepts any `IntoIterator<Item = Row>`.
- Proof: The same four row literals are produced in the same order with the same cell text and styles; each call still constructs fresh `Row` values for the table to own, and only the unnecessary fixture vector allocation is removed. Selection, theme choice, header rendering, Markdown preview behavior, floating-point phase, RNG, and tie-breaking are unchanged. A focused test proves the fixture remains fixed-size and renders row labels in order.
- Measurement: No fresh profile in-pass; final profile refresh follows this pass.
- Verification: `rustfmt --edition 2024 --check crates/ftui-demo-showcase/src/screens/table_theme_gallery.rs`; `git diff --check -- crates/ftui-demo-showcase/src/screens/table_theme_gallery.rs`; `RCH_REQUIRE_REMOTE=1 rch exec --json -- cargo test -p ftui-demo-showcase --lib table_rows_are_fixed_size_and_render_in_order -- --nocapture`; `RCH_REQUIRE_REMOTE=1 rch exec --json -- cargo clippy -p ftui-demo-showcase --all-targets -- -D warnings`; no local Cargo fallback occurred.
- Commit: `a482a43e`
- Verdict: PRODUCTIVE

## Final Post-Pass-10 Profiling Refresh - 2026-06-12T17:00:07Z

- Run directory: `tests/artifacts/perf/20260612T165631Z-post-pass10-final/`
- Command: `RCH_REQUIRE_REMOTE=1 rch exec --json -- cargo run --profile release-perf -p ftui-demo-showcase --bin profile_sweep -- --cycles 200 --render-mode pipeline --arena-mode off --json`
- Remote worker: `vmi1149989`; no local fallback occurred.
- Summary: `18,000` frames, elapsed `4940.170ms`, throughput `3643.60 renders/sec`, frame p50/p95/p99 `212/631/998us`, allocation p50/p95/p99 `163/730/1194`, bytes p95 `501,286`, presenter p95/p99 `73/98us`.
- Compared with baseline: frame p95/p99 improved from `701/1479us` to `631/998us`; allocation p95/p99 improved from `966/6191` to `730/1194`; throughput improved from `3231.9` to `3643.6 renders/sec`.
- Compared with the post-pass-5 refresh: frame p95/p99 improved from `992/2264us` to `631/998us`; allocation p95 improved from `773` to `730`; presenter p95/p99 improved from `84/135us` to `73/98us`.
- Final allocation tail: `dashboard` p95 `1194`, `markdown_rich_text` p95 `1155`, `code_explorer` p95 `954`, `table_theme_gallery` p95 `855`, `theme_studio` p95 `827`.
- Final frame-time tail: `quake_easter_egg` p95 `1215us`, `visual_effects` p95 `1087us`, `dashboard` p95 `970us`, `markdown_rich_text` p95 `793us`.

## Final Verification - 2026-06-12T17:16:02Z

- Passed: `rustfmt --edition 2024 --check` on the Rust files changed since `origin/main`.
- Passed: `git diff --check origin/main..HEAD`.
- Passed remotely: `RCH_REQUIRE_REMOTE=1 rch exec --json -- cargo check --workspace --all-targets` on `vmi1152480`.
- Passed remotely: `RCH_REQUIRE_REMOTE=1 rch exec --json -- cargo clippy --workspace --all-targets -- -D warnings` on `vmi1149989`.
- Full remote workspace test note: `RCH_REQUIRE_REMOTE=1 rch exec --json -- cargo test --workspace` on `vmi1153651` failed only in `ftui-demo-showcase/tests/async_tasks_stress.rs` latency gates (`regression_gate_render_latency`, `stress_view_render_with_many_tasks`), with p95 render latency slightly over the `10ms` budget on that run.
- Passed remotely on retry: `RCH_REQUIRE_REMOTE=1 rch exec --json -- cargo test -p ftui-demo-showcase --test async_tasks_stress -- --nocapture` on `vmi1152480`, including the two previously failed latency gates at p95 `2.00ms` and `2.29ms`.
- No local Cargo build, check, clippy, test, run, profile, or bench command was executed.

## Continuation Pass 11 - Inline Flex constraint storage - 2026-06-13T00:24:00Z

- Bead: `bd-s5m2a`.
- Files changed: `crates/ftui-layout/src/lib.rs`.
- Changes: Replaced `Flex`'s private `Vec<Constraint>` storage with the existing `SmallVec<[Constraint; LAYOUT_INLINE_CAP]>` pattern already used for `Rects` and solver scratch sizes, so common small fixed-array layout calls avoid one heap allocation while large constraint lists still spill transparently.
- Evidence: Fresh current-tree profile before this patch reported allocation p50/p95/p99 `163/730/1194`; `Flex::constraints([...])` is used per-frame across many demo screens with 2-5 constraints, and `ftui-layout` already documents an inline layout capacity of 8 for the common case.
- Proof: Constraint order and values are unchanged; `SmallVec` derefs to the same slice accepted by the solver; split, alignment, margin, gap, overflow, RTL mirroring, intrinsic sizing, coherence IDs, and spill behavior keep the same semantics. A focused unit test proves an 8-constraint common layout remains inline and still produces eight one-cell rects.
- Measurement: Post-change remote `profile_sweep --cycles 200 --render-mode pipeline --arena-mode off --json` on `vmi1149989` improved allocation p50/p95/p99 from `163/730/1194` to `157/718/1179`; dashboard p95 allocations improved from `1194` to `1179`. Frame p95 was noisy (`523us` before, `558us` after), so this is counted as an allocation-tail win only.
- Verification: `rustfmt --edition 2024 --check crates/ftui-layout/src/lib.rs`; `git diff --check -- crates/ftui-layout/src/lib.rs`; `RCH_REQUIRE_REMOTE=1 rch exec --json -- cargo test -p ftui-layout --lib flex_constraints_stay_inline_for_common_layouts -- --nocapture`; `RCH_REQUIRE_REMOTE=1 rch exec --json -- cargo check --workspace --all-targets`; `RCH_REQUIRE_REMOTE=1 rch exec --json -- cargo clippy --workspace --all-targets -- -D warnings`. The remote check and clippy commands exited 0; both artifact retrievals later warned on rsync cache retrieval, which does not invalidate the remote command exit status. No local Cargo build, check, clippy, test, run, profile, or bench command was executed.
- Commit: `1d9a40b7`
- Verdict: PRODUCTIVE
